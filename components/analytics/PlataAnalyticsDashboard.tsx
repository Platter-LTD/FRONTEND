"use client"

/**
 * Legacy export — analytics UI is now Product overview.
 * Prefer importing `ProductOverviewDashboard` directly.
 */
export { default } from "@/components/analytics/ProductOverviewDashboard"
